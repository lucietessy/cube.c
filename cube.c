//program to find cube up to a geven interger
#include <stdio.h.>
int main ()
{
    int x=1;
    do
    {printf("%d\n",x);
    x=x+1;
    printf("number is %d and cube of the %d is is,\n",x,x,(x*x*x));
    while(x<=3);
    return 0;
}